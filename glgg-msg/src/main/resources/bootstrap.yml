spring:
  application:
    name: glgg-msg
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
  rabbitmq:
    host: 192.168.0.104
    port: 5672
    username: guest
    password: guest

server:
  port: 6002
  
  
