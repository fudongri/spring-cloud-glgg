spring:
  application:
    name: glgg-user
  profiles:
    active: dev

server:
  port: 1001

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

